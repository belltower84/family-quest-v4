rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Sprint 1 safe default: deny all access until parent membership documents
    // and the final family schema are deployed.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
